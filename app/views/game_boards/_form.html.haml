= form_for @game_board do |f|
  - if @game_board.errors.any?
    #error_explanation
      %h2= "#{pluralize(@game_board.errors.count, "error")} prohibited this game_board from being saved:"
      %ul
        - @game_board.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
