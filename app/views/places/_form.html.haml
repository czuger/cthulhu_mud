= form_for @place do |f|
  - if @place.errors.any?
    #error_explanation
      %h2= "#{pluralize(@place.errors.count, "error")} prohibited this place from being saved:"
      %ul
        - @place.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :default_translation
    = f.text_field :default_translation
  .field
    = f.label :parent_id
    = select( :place, :parent_id, Place.all.map { |p| [ p.translated_name, p.id ] }, {include_blank: 'None'} )
  .field
    = f.label :Masculin
    = f.radio_button( :gender, :m )
    = f.label :Feminin
    = f.radio_button( :gender, :f )
    = f.label :Plural
    = f.radio_button( :gender, :p )

  = f.submit 'Save'
